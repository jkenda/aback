macro add s32 s32 -> s32 is
    +
end

let thirteen := add 7 6 in
puts "7 + 6 = " |>
puti thirteen |>
putc '\n'

func max
    s32 s32
    -> s32
is
    take a b in
        if >= a b then a
        else b end
    end
end

